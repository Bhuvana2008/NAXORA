/**
 * ==========================================================================
 * NAXORA - Production & Deployment Configuration
 * ==========================================================================
 * 
 * Set your production backend API URL below when hosting the frontend separately (e.g. on Netlify).
 * If left empty, NAXORA automatically defaults to the relative '/api' endpoint or localhost.
 * 
 * Example:
 * window.NAXORA_CONFIG = {
 *     API_BASE_URL: 'https://naxora-backend.onrender.com/api'
 * };
 */

window.NAXORA_CONFIG = window.NAXORA_CONFIG || {
    // API_BASE_URL: 'https://your-production-backend-url.com/api'
};
